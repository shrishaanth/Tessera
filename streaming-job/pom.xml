<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.tessera.risk</groupId>
        <artifactId>tessera-risk</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>streaming-job</artifactId>
    <name>Tessera Risk :: Streaming Job</name>
    <description>Spark Structured Streaming windowed risk aggregation into HBase</description>

    <dependencies>
        <dependency>
            <groupId>com.tessera.risk</groupId>
            <artifactId>common</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Spark itself is supplied by the runtime (spark-submit, or the Spark
             container's own jars), so it must not be shaded into the job jar:
             two copies of Spark on one classpath is a reliable way to get
             confusing NoSuchMethodErrors. Provided scope still puts it on the
             compile and exec:java classpaths, so `mvn exec:java` runs the job in
             local mode from a developer machine without a second profile. -->
        <dependency>
            <groupId>org.apache.spark</groupId>
            <artifactId>spark-core_${scala.binary.version}</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.spark</groupId>
            <artifactId>spark-sql_${scala.binary.version}</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- The Kafka source is NOT part of a Spark distribution; it has to be
             supplied by the application, either by the spark-submit packages
             option or, as here, inside the job jar so the job stays runnable
             with no network. -->
        <dependency>
            <groupId>org.apache.spark</groupId>
            <artifactId>spark-sql-kafka-0-10_${scala.binary.version}</artifactId>
        </dependency>

        <!-- Alerts are published with a plain producer from the driver, not
             through a Spark sink — see AlertPublisher for why. -->
        <dependency>
            <groupId>org.apache.kafka</groupId>
            <artifactId>kafka-clients</artifactId>
        </dependency>

        <!-- hbase-shaded-client, not hbase-client: the shaded artifact relocates
             its Guava, Protobuf and Netty so they cannot collide with the
             different versions Spark puts on the same classpath. That collision
             is the single most common reason an HBase-from-Spark job fails. -->
        <dependency>
            <groupId>org.apache.hbase</groupId>
            <artifactId>hbase-shaded-client</artifactId>
            <version>${hbase.version}</version>
            <exclusions>
                <!-- Spark already brings its own logging bridge; a second one
                     causes the multiple-SLF4J-bindings warning and non-deterministic
                     log routing. -->
                <exclusion>
                    <groupId>org.slf4j</groupId>
                    <artifactId>slf4j-log4j12</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>log4j</groupId>
                    <artifactId>log4j</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>

        <!-- Pinned to provided so they stay out of the job jar. All three arrive
             at compile scope through the Kafka connector, but every one of them is
             already on the classpath of any Spark runtime, and hbase-shaded-client
             carries its own copy of the Hadoop classes besides. Left alone they add
             about 35 MB of duplicates to the jar, which spark-submit then ships to
             every executor for nothing. -->
        <dependency>
            <groupId>org.apache.hadoop</groupId>
            <artifactId>hadoop-client-runtime</artifactId>
            <version>${hadoop.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.scala-lang</groupId>
            <artifactId>scala-library</artifactId>
            <version>2.12.18</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.google.code.findbugs</groupId>
            <artifactId>jsr305</artifactId>
            <version>3.0.0</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!--
                Spark reaches into JDK internals by reflection, which the module
                system has denied by default since Java 16: Tungsten's off-heap
                memory goes through sun.nio.ch.DirectBuffer, and the Java
                serializer needs java.lang and java.util opened. Without these the
                first Spark call fails as "Could not initialize class
                org.apache.spark.storage.StorageUtils$", which names none of that.

                spark-submit adds these itself, which is why the containerised job
                needs no equivalent. A JVM that embeds Spark directly, like a
                forked test JVM, has to supply them. This is the same list Spark's
                own launcher applies.
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <argLine>
                        --add-opens=java.base/java.lang=ALL-UNNAMED
                        --add-opens=java.base/java.lang.invoke=ALL-UNNAMED
                        --add-opens=java.base/java.lang.reflect=ALL-UNNAMED
                        --add-opens=java.base/java.io=ALL-UNNAMED
                        --add-opens=java.base/java.net=ALL-UNNAMED
                        --add-opens=java.base/java.nio=ALL-UNNAMED
                        --add-opens=java.base/java.util=ALL-UNNAMED
                        --add-opens=java.base/java.util.concurrent=ALL-UNNAMED
                        --add-opens=java.base/java.util.concurrent.atomic=ALL-UNNAMED
                        --add-opens=java.base/sun.nio.ch=ALL-UNNAMED
                        --add-opens=java.base/sun.nio.cs=ALL-UNNAMED
                        --add-opens=java.base/sun.security.action=ALL-UNNAMED
                        --add-opens=java.base/sun.util.calendar=ALL-UNNAMED
                        --add-opens=java.security.jgss/sun.security.krb5=ALL-UNNAMED
                        -Djdk.reflect.useDirectMethodHandle=false
                    </argLine>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.2.0</version>
                <configuration>
                    <mainClass>com.tessera.risk.streaming.StreamingMain</mainClass>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals><goal>shade</goal></goals>
                        <configuration>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>com.tessera.risk.streaming.StreamingMain</mainClass>
                                </transformer>
                                <!-- Spark discovers the Kafka source through
                                     META-INF/services/...DataSourceRegister. Without
                                     this transformer the shade plugin keeps only one
                                     services file and the source vanishes, failing at
                                     run time with "Failed to find data source: kafka". -->
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                            </transformers>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                        <exclude>module-info.class</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
